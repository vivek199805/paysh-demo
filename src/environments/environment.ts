// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // apiBaseUrl: 'https://uat.rnfiservices.in/apiportal/api/v1/'
  // apiBaseUrl: 'https://devops.rnfi.in/apiportal/api/v1/'
  //apiBaseUrl   :'https://amritmoney.com/api/api/v1/',  
  //apiBaseUrl: 'https://devops.rnfi.in/apiportal/v1/api/v1/'
   apiBaseUrl: 'https://api.rnfi.in/paysh/api/api/v1/' ,  
  //  apiBaseUrl: 'https://pigopay.in/api/api/v1/' 
  // apiBaseUrl   :'https://bharatpay.info/api/api/v1/'

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
